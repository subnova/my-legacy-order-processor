Spring Cloud Streams Simple Message Processor
=============================================

Simple Spring Boot / Spring Cloud Streams application that transforms a message received from a Kafka queue.

To build:

$ ```./gradlew build```

To run:

See https://github.com/subnova/my-order-service

